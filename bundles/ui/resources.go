package ui

// Frame collects the complete UI declaration for one update tick.
type Frame = frame

// Interactions contains the results from the most recently processed UI frame.
type Interactions = interactions
